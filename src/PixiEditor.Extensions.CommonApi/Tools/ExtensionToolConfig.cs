namespace PixiEditor.Extensions.CommonApi.Tools;

public class ExtensionToolConfig
{
    public CustomToolConfig Config { get; }

    public ExtensionToolConfig(CustomToolConfig config)
    {
        Config = config;
    }
}
